# ================================
# JWT Configuration
# ================================
# IMPORTANT: Generate a secure secret with: python -c "import secrets; print(secrets.token_urlsafe(32))"
# Must be at least 32 characters long
JWT_SECRET=CHANGE_THIS_TO_A_SECURE_RANDOM_STRING_OF_AT_LEAST_32_CHARACTERS_LONG
JWT_ALG=HS256
JWT_TTL=900

# ================================
# Database Configuration
# ================================
# These are the default values for H2 in the docker-compose setup
# The backend connects to H2 via TCP (h2:9092)
DB_USER=sa
DB_PASSWORD=
# Note: DB_URL is set in docker-compose.yml to point to the h2 container

# ================================
# CORS Configuration
# ================================
# For production, set this to your actual domain
# For local testing with docker, use http://localhost or http://localhost:80
CORS_ORIGINS=http://localhost,http://localhost:80

# ================================
# Logging
# ================================
LOG_LEVEL=INFO

# ================================
# Admin User Seeding (Optional)
# ================================
# If set, an admin user will be created on first startup
# Leave empty to skip admin user creation
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=ChangeThisAdminPassword123!

# ================================
# Frontend Configuration (Optional)
# ================================
# If you need to override the API base URL for the frontend
# Leave empty to use relative URLs (recommended for Mode A deployment)
# VITE_API_BASE_URL=
